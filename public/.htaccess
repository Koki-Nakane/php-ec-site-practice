RewriteEngine On

# Allow existing files or directories to be served directly
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Otherwise route everything to the front controller
RewriteRule ^ index.php [QSA,L]
